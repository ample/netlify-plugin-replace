name: netlify-plugin-replace
inputs:
  - name: delimiter
    description: Pattern encapsulating strings for replacement
    default: \${([^}]*)}
  - name: fileTypes
    description: Pattern to filter files by
    default: .*$
